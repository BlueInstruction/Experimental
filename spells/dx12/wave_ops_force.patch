diff --git a/src/freedreno/vulkan/tu_shader.cc b/src/freedreno/vulkan/tu_shader.cc
--- a/src/freedreno/vulkan/tu_shader.cc
+++ b/src/freedreno/vulkan/tu_shader.cc
@@ -1,3 +1,4 @@
+/* Dragon: Wave Ops Force for UE5/DX12 compatibility */
 
 /* Force wave operations enabled for shader model 6.0+ support */
 
@@ -50,9 +51,9 @@ tu_shader_init_subgroup_info(struct tu_shader *shader)
    /* Subgroup configuration for DX12 WaveOps */
-   .subgroupSize = 64,
+   .subgroupSize = 32,
-   .minSubgroupSize = 64,
+   .minSubgroupSize = 32,
-   .maxSubgroupSize = 128,
+   .maxSubgroupSize = 64,
    
    /* Force wave operations support */
    .subgroupSupportedStages = VK_SHADER_STAGE_ALL,
    .subgroupSupportedOperations = 
        VK_SUBGROUP_FEATURE_BASIC_BIT |
        VK_SUBGROUP_FEATURE_VOTE_BIT |
        VK_SUBGROUP_FEATURE_ARITHMETIC_BIT |
        VK_SUBGROUP_FEATURE_BALLOT_BIT |
        VK_SUBGROUP_FEATURE_SHUFFLE_BIT |
        VK_SUBGROUP_FEATURE_SHUFFLE_RELATIVE_BIT |
        VK_SUBGROUP_FEATURE_QUAD_BIT,
